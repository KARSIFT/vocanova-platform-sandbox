schema_version: 1
task_id: VOC-113-T02
ownership: operator

# Read-only verifier added by T00 (VOC-113-D12). It resolves promotion PR #947's
# merge result and proves the expected post-promotion workflow succeeded for that
# exact SHA. The verifier run itself is bound to the T02 evidence-carrier head.
workflow_file: pipeline.yml
workflow_name: pipeline
job_names:
  - verify-post-promotion-workflow / verify
events:
  - workflow_dispatch
branch: agent/voc-113-voc-113-t02
sha_lineage:
  mode: exact_pr_head
conclusion: success
max_age: 72h

# Operator dispatches pipeline.yml directly on the exact carrier branch with
# action=verify-post-promotion-workflow and promotion_pr_number=947. The
# reconciler observes the result but intentionally never dispatches pipeline.yml,
# preventing self-recursion through the pipeline workflow.
