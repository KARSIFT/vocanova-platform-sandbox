schema_version: 1
task_id: VOC-113-T01
ownership: operator

# Read-only verifier added by T00 (VOC-113-D10). Proves promotion PR #947 has
# genuine required exact-head checks without fabricating statuses or merging.
workflow_file: pipeline.yml
workflow_name: pipeline
job_names:
  - verify-promotion-check-recovery / verify
events:
  - workflow_dispatch
branch: agent/voc-113-voc-113-t01
sha_lineage:
  mode: exact_pr_head
conclusion: success
max_age: 72h

# Operator dispatches pipeline.yml directly on the exact carrier branch with
# action=verify-promotion-check-recovery and promotion_pr_number=947. The
# reconciler observes the result but intentionally never dispatches pipeline.yml,
# preventing self-recursion through the pipeline workflow.
