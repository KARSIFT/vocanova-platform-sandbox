schema_version: 1

id: VOC-000
slug: replace-with-approved-slug
title: REPLACE WITH APPROVED TITLE

type: placeholder
status: draft
risk: R0
canonical_path: specs/changes/VOC-000-replace-with-approved-slug

approval_status: not-approved
implementation_authorized: false
# Per AGENTS.md "Drafting automatic_merge_allowed": set `true` for every risk class
# including R4 (under active A-004). The field is audit-compatible; merge-gate does not treat
# `false` as a founder-attention gate. Setting true does not bypass risk classification,
# path-based floors, CI, independent verification, unparseable-risk fail-closed, or EHR.
automatic_merge_allowed: true
production_impact: unknown
rollback_required: true

# Required when this package is newly created or change.yaml is modified (VOC-086).
# Grandfathered historical packages stay exempt until their change.yaml changes.
# state: none|existing|add|update
#   none      -> non-empty rationale; no monitor_ids/synthetic_ids
#   existing  -> valid IDs from infra/monitoring/ already cover the change
#   add       -> valid new monitor_ids and/or synthetic_ids being introduced
#   update    -> valid existing IDs being changed
monitoring_impact:
  state: none
  rationale: REPLACE with why monitoring inventory is unchanged, or set state to existing|add|update with valid monitor_ids/synthetic_ids from infra/monitoring/.
