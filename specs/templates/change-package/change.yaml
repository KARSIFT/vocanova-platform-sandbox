schema_version: 1

id: VOC-000
slug: replace-with-approved-slug
title: REPLACE WITH APPROVED TITLE

type: placeholder
status: draft
risk: R0
canonical_path: specs/changes/VOC-000-replace-with-approved-slug

approval_status: not-approved
implementation_authorized: false
# Per AGENTS.md "Drafting automatic_merge_allowed": set per declared risk class
# before the plan PR is reviewed — do not leave this as an unexamined inherit.
# R0–R3: true always. Do not set false to require founder eyes on merge;
# sensitivity (auth, secrets, production infrastructure, or similar) does not
# justify a non-R4 opt-out. R4: false (redundant with merge-gate's R4 hard
# block; keeps the package record self-describing). Setting true does not bypass
# risk classification, path-based floors, CI, independent verification, R4
# founder authority, or EHR.
automatic_merge_allowed: true
production_impact: unknown
rollback_required: true
