# OpenCode agents

OpenCode-specific agent definitions go here, translated from the canonical
prose in `.agents/agents/`. Empty until there's an agent worth sharing
across tools — see `.claude/commands/` and `.codex/agents/` for the same
convention on the other tools.
