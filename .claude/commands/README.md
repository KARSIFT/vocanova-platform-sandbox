# Claude Code slash commands

Project-scoped slash commands go here (`.claude/commands/<name>.md`). Empty
until there's a command worth adding — see `.codex/agents/` and
`.cursor/rules/` for the same convention on the other tools.
