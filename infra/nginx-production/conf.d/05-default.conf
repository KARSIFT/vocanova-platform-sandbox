# infra/nginx-production/conf.d/05-default.conf
#
# VOC-067-T02: catch-all default_server for the shared edge lives in
# infra/nginx-shared/conf.d/05-default.conf (loaded once for both tiers).
# This file intentionally contains no server blocks so production's conf
# tree does not collide with staging's historical 05-default.conf when
# both tiers mount into vocanova-shared-edge-nginx.
