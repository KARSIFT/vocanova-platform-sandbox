# Cursor rules

Cursor-specific `.mdc` rules go here, translated from the canonical prose
in `.agents/agents/` and `.agents/skills/`. Empty until there's a rule
worth sharing across tools — see `.claude/commands/` and `.codex/agents/`
for the same convention on the other tools.
